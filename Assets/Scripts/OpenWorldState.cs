public static class OpenWorldState
{
    // true = menu do mundo aberto está aberto
    public static bool menuAberto = true;

    // true = veio do minigame e deve voltar jogando
    public static bool voltandoDoMinigame = false;
}
